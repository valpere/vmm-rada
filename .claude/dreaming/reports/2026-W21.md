# llm-council dreaming pass — 2026-W21

Date: 2026-05-24
Branch surveyed: main
Commits examined: 33 (since 2026-04-24, 30-day window); only 4 since W20 (2026-05-17) and all are dep bumps
PRs examined: 10 (PRs #218, #221, #222, #223 merged since W20 — all dependabot; #219/#220 closed without merge; #211/#213/#215/#217 re-inspected for W20 finding persistence)

## TL;DR
- **W21 is a "ditto" pass.** All 4 substantive findings from W20 are still present verbatim on `main`; nothing has been applied. Since 2026-05-17 only dependabot PRs (#218, #221, #222, #223) merged. No fix-review commits, no W20 follow-up issues opened, no agent-memory edits.
- **W19 → W20 → W21 trend**: dreaming reports stack up faster than they're applied. W19 was partially applied via #207 (one of four agent-memory items); W20 is unapplied; W21 will add a third report to the backlog. Suggests `/apply-dreaming` needs to be invoked weekly, or the report cadence should slow.
- **One new low-priority finding**: dependabot batched 6 PRs over the past 30 days (#218, #220 closed, #221, #222, #223, plus #218 react-dom) but the 2 react-* PRs (#219, #220) were closed un-merged. Worth a one-line note from `/review-deps`.
- **Critical pre-existing drift (re-affirmed)**: `frontend/src/components/Stage2.jsx:426,455` still renders MoA proposer/aggregator content as raw JSX expressions (violates context-essentials rule 4). `frontend/src/App.jsx:12-21` `deriveStage2Kind` still only knows `default → peer_ranking` despite 6 strategies persisting since.

## 1. Context-essentials drift

Same checks as W20, run against current `main` (`7738da9`).

- **Rule 4 — "`react-markdown` is the only renderer for LLM output"** — ❌ **DRIFT persists**.
  - `frontend/src/components/Stage2.jsx:426` (`{result.content}` in `MoaProposerRow`) and `:455` (`{aggregator.content}` in `MoaAggregatorRow`). No change since W20.
  - Severity: medium. W20 flagged; no commit touches Stage2.jsx since `7738da9` (Delphi PR #217, 2026-05-10).
  - Suggest: still file a `frontend` bug — replace with the shared `<Markdown>` wrapper. (Already actionable; W20's recommendation stands.)

- **Rule "`App.jsx` owns all state"** — ✅ holds. `grep -n setCurrent frontend/src/components/` returns no hits.

- **Rule "Components are pure UI; no `fetch` / `api.js` calls"** — ✅ holds.

- **Rule "No raw HTML rendering of LLM output"** (`dangerouslySetInnerHTML`) — ✅ holds.

- **Rule "No `fmt.Println` in `cmd/`"** — ✅ holds.

- **Rule "No `--no-verify`"** — ✅ holds.

- **Rule "Plans deleted after issue creation"** — ✅ holds. `.claude/plans/` contains only `README.md` (last touched 2026-03-31 per `ls -la`).

- **Rule "No TypeScript in `frontend/`"** — ✅ holds.

Confidence: **high** — same drift as W20, no new violations introduced.

## 2. Recurring /fix-review themes

No new PR review data: only dependabot PRs merged since W20, and `gh pr view 215/217/213 --json comments --jq '.comments | length'` returns 0 (Copilot comments are surfaced via the reviews API, not the comments API — but the substance is unchanged from W20's detailed audit).

All four W20 themes still apply unmodified:

- **Cross-cutting strategy contracts skipped during fan-out** — re-verified:
  - `mergeRoundIntoMessage` shallow-clone bug: `frontend/src/App.jsx:45-59`. Line 47 still does `{ ...event.metadata }` — shared inner refs. Confirmed.
  - `deriveStage2Kind`: `frontend/src/App.jsx:12-21` still has `default → peer_ranking` and no cases for the 6 new strategies. Confirmed.
  - `ConsensusW=0.0` hardcoded: `internal/council/debate.go:133,163`, `internal/council/delphi.go:166,210`, `internal/council/moa.go:103`. Confirmed via grep. (Majority, RankRefine, RoleBased compute or document a real value.)
  - `AggregatorOutput.Error error \`json:"-\"\`` — not re-checked but no PR has touched `moa.go` since #215.
  - Confidence: **high**.

- **Prompt format-string count mismatches** — both instances still present:
  - `internal/council/prompts.go:616` — `"%d raters scored %d candidate answers across %d rounds"` formatted with `len(criteria)` as the third arg.
  - `internal/council/prompts.go:749` — `"An arbiter ranked %d council answers"` formatted with `len(advancing)` (the post-arbitration top-K, not the original N council answers).
  - Confidence: **medium**.

- **Doc-comment vs runtime contract drift** — unchanged.

- **Meta-theme: `/fix-review` not invoked** — re-confirmed. `git log --grep="fix-review" --grep="address Copilot" --grep="address review" --all` returns 2 commits, both internal to PR #207 ("apply dreaming W19 findings", May 10 — `285cfc2` and `ba839a3`). No PR since #207 has a fix-review-style follow-up commit. Confidence: **high**.

## 3. Stale plans

- ✅ Clean. `.claude/plans/` contains only `README.md`. "Delete plan after issue creation" still followed.

## 4. Agent-memory health

Re-checked W20's three findings; all unmodified.

- **`tech-lead/known_issues.md`** — mtime `2026-03-22 11:37`, now **63 days old**.
  - Same three Critical items un-annotated:
    - "No API key validation at startup" — not verified against current `config/config.go`
    - "Stage3SynthesizeFinal never returns an error" — not verified
    - "No graceful shutdown" — known-resolved per `tech-lead/project_architecture.md` (the apply pass updated only that file)
  - Also still contradicts `project_architecture.md` on CORS (resolved per #31 according to known_issues line 43, but architecture file flagged the previous claim as wrong).
  - Severity: **medium**. Reasoning agent will load this and reason from outdated state.

- **`go-security-reviewer/project_frontend_security_posture.md`** — mtime `2026-03-31 14:06`, **54 days old**. Still references issues #53 and #54 as "Known open issues" — both closed. Severity: low.

- **`tech-lead/project_architecture.md`** — mtime `2026-05-10 10:35`, 14 days old. ✅ Fresh. W19 apply did this one.

- **`code-generator/` and `code-simplifier/` agent-memory dirs**: still empty 54+ days after creation. The agents don't appear to write memories. Either remove or document the convention. Severity: low.

- **No new contradictions** introduced since W20.

## 5. Skill / agent inventory

- **Agents (11)**: unchanged from W20. Most recent edit `2026-05-10` (security-reviewer + go-security-reviewer scope disambiguation). No new agents, no removed agents.

- **Skills (11)**: `apply-dreaming`, `backlog`, `find-bugs`, `fix-review`, `housekeeping`, `improve`, `lib`, `review-deps`, `revival`, `self-learn`, `ship`.
  - `lib/` contains only `env.sh` + `rest.sh` (helper scripts, not a real skill). W20 flagged for folding-elsewhere decision; unchanged.
  - `fix-review` still unused in practice (see §2 meta-theme).
  - Most recent skill edit: `apply-dreaming` on 2026-05-10 (#208).

- **Dreaming reports**: `2026-W21.md` was already created as a 0-byte placeholder before this run (matches W20's observation about the cron-creates-then-writer pattern; presumably the systemd timer touched it).

## 6. Patterns I noticed

- **Dreaming backlog is growing.** W19 produced 4 agent-memory findings → 1 applied (project_architecture.md only). W20 produced 4 high/medium findings → 0 applied at the time of W21. W21 is mostly a restatement. The flywheel needs `/apply-dreaming` invoked manually between report production and the next pass, otherwise the reports decay into duplicates. If apply-dreaming is supposed to be triggered by the same systemd timer, the wiring isn't there.

- **The repo entered a maintenance lull.** From 2026-05-10 (Delphi merged) through 2026-05-24, the only landed commits are dependabot. That's a 14-day quiet window after a 1-day burst of 5 strategy PRs. This is fine, but it means W21's window contains no new substantive work to audit — the report would have been near-empty if W20 hadn't accumulated unaddressed findings.

- **The dependabot loop is reasonably healthy.** 4 of 6 dep bumps merged cleanly in the past 14 days; 2 react-* PRs (#219, #220) were closed without merge — worth a one-line check in the `/review-deps` flow to log *why* they were closed (superseded by #218? rejected?).

## 7. What I couldn't tell (need manual review)

- Whether the systemd `apply-dreaming` companion timer exists or is intended. If `/apply-dreaming` is meant to be manual, the dreaming README should note the cadence the user actually follows ("apply weekly", "apply opportunistically"). If automatic, the wiring needs to be added.
- Whether the W19/W20/W21 findings are deliberately deferred (Val has decided they're not worth fixing right now) or simply unread. The empty annotation history makes both interpretations equally valid from outside.
- Why react@19.2.4 → 19.2.6 (#220) and vite 8.0.12 (#219) were closed un-merged while react-dom@19.2.6 (#218) and vite@8.0.13 (#222) merged a week later. Likely "superseded by a later bump" but worth confirming `/review-deps` is the entity doing this.
- Whether the dependabot-only window since 2026-05-10 represents a deliberate planning pause (eval-harness work, design retro) or just calendar gaps.
