# vmm-rada dreaming pass — 2026-W20

Date: 2026-05-17
Branch surveyed: main
Commits examined: 49 (since 2026-02-17, 30-day window contains 33 of these)
PRs examined: 10 most recent (Copilot reviews on #206, #211, #213, #215, #217 inspected in detail)

## TL;DR
- **Active context-essentials drift**: `frontend/src/components/Stage2.jsx:426,455` renders MoA proposer/aggregator drafts as **raw `{result.content}` / `{aggregator.content}`**, not via `<Markdown>`. Violates rule 4 ("`react-markdown` is the only renderer for LLM output"). Flagged by Copilot in PR #215 — merged unfixed. Severity: medium.
- **Conversation replay is broken for 6 of 7 strategies**: `frontend/src/App.jsx:12-21` `deriveStage2Kind` only knows `default → peer_ranking`. After a reload, Debate/Delphi/MoA/Majority/RankRefine/RoleBased conversations all render as PeerReview. Flagged by Copilot in PR #213, never addressed; the same defect class now applies to 6 strategies.
- **`/fix-review` is prescribed but not invoked**: 0 commits in the last 30 days reference `fix-review`, "address Copilot", or "address review". 22 Copilot comments across 5 strategy PRs (#206/211/213/215/217) were merged without any consolidated-fix commit. The skill ships but the workflow isn't running.
- **`tech-lead/known_issues.md` still 56 days stale**: W19 flagged it; W19 apply (#207) refreshed `project_architecture.md` but skipped this file. The three "Critical" items remain un-annotated.

## 1. Context-essentials drift

Re-ran the W19 baseline checks plus tracing of newly merged strategy PRs.

- **Rule 4 — "`react-markdown` is the only renderer for LLM output"** — ❌ **DRIFT (medium-high)**.
  - Evidence: `frontend/src/components/Stage2.jsx:426` (`{result.content}` for `MoaProposerRow`) and `:455` (`{aggregator.content}` for `MoaAggregatorRow`). Stage2.jsx imports no Markdown component; `grep -l "Markdown\\|react-markdown" frontend/src/components/*.jsx` returns Stage0/1/3 + ChatInterface + Sidebar — Stage2 is the only LLM-output renderer that opted out.
  - Copilot flagged this twice in PR #215; comments were left unresolved at merge (commit `fbbe3a8`).
  - Not strictly an XSS bug (React auto-escapes `{value}`), but breaks the project's rendering-consistency contract and produces visibly different output for users (code fences, lists, bold all become plaintext).
  - Suggest: file a `frontend` bug — replace plaintext rendering with the shared `<Markdown>` wrapper used in Stage1/Stage3.

- **Rule "`App.jsx` owns all state via `setCurrentConversation`"** — ✅ holds. `grep setCurrent frontend/src/components/` returns no hits; the only match is `frontend/src/App.jsx`.

- **Rule "Components are pure UI; no `fetch` / `api.js` calls"** — ✅ holds. `grep fetch( frontend/src/components/` returns nothing.

- **Rule "No raw HTML rendering of LLM output"** — ✅ holds. `grep dangerouslySetInnerHTML frontend/src` returns no hits.

- **Rule "No `fmt.Println` in `cmd/`"** — ✅ holds. `grep --include='*.go' fmt.Println` returns nothing in the whole tree.

- **Rule "No `--no-verify`"** — ✅ holds (only matches in dreaming prompt itself).

- **Rule "Plans deleted after issue creation"** — ✅ holds. `.claude/plans/` contains only `README.md`.

- **Rule "No TypeScript in `frontend/`"** — ✅ holds.

Confidence: **high** — one confirmed drift, all other rules clean.

## 2. Recurring /fix-review themes

Sampled Copilot review bodies + per-file inline comments on PRs #206, #211, #213, #215, #217. **`/fix-review` itself was not invoked** on any of these — themes below are recurring *unaddressed* Copilot findings that the skill is meant to catch.

- **Theme: cross-cutting strategy contracts skipped during strategy fan-out** — n=4 confirmed instances across 5 strategy PRs (~3 weeks).
  - Evidence-A: `mergeRoundIntoMessage` shallow-clone bug. `frontend/src/App.jsx:45-59` — `msg.metadata = { ...event.metadata }` shares the `.rounds` array with `event.metadata`, then `push(...incoming)` mutates both. Flagged in PR #213 (Debate) and again in PR #217 (Delphi). Unfixed in both. Still present on `main`.
  - Evidence-B: `deriveStage2Kind` only handles `default → peer_ranking`. `frontend/src/App.jsx:12-21`. Flagged in PR #213. With 6 new strategies merged since, the replay path silently downgrades all of them to PeerReview view.
  - Evidence-C: `ConsensusW` is hardcoded `0.0` in `delphi.go:166,210`, `moa.go:103`, `debate.go:133,163`; flagged for Majority in PR #206 ("incorrectly report perfect consensus"). Majority fixed (`majority.go:60-79` uses winnerVotes/totalVotes); GenerateRankRefine fixed (`generaterankrefine.go:100` uses `averageScoreOf`); the other three were never reconsidered. Eval harness reads `Metadata.ConsensusW` so these three strategies report 0 consensus regardless of agreement.
  - Evidence-D: `AggregatorOutput.Error error \`json:"-"\`` (PR #215) — failed aggregators emit empty content with no failure reason on the wire. Pattern may apply to other strategies that wrap LLM calls.
  - Suggest: when fanning out a strategy family, run a checklist sweep over (a) frontend dispatcher, (b) replay derivation, (c) cross-cutting metrics (`ConsensusW`), (d) Markdown rendering, (e) failure surfacing. Candidate for a new `tech-lead` checklist item or a "strategy-fanout" agent.
  - Confidence: **high**.

- **Theme: prompt format-string count mismatches** — n=2 distinct PRs.
  - PR #217: `BuildDelphiChairmanPrompt` formats `"%d raters scored %d candidate answers across %d rounds"` with `len(criteria)` as the third arg (number of criteria, not rounds). Flagged, unfixed.
  - PR #211: `"An arbiter ranked %d council answers..."` formatted with `len(advancing)` rather than full N.
  - Suggest: simple `go vet` won't catch semantic mismatches. Tech-lead checklist line — "Inspect every `%d` / `%s` substitution against the surrounding sentence."
  - Confidence: medium.

- **Theme: doc-comment vs runtime contract drift** — n=3+ instances.
  - PR #206: `MajorityCouncilTypeChairman` doc comment claims "ties cause a loud error when empty", but `cmd/server/main.go` and `cmd/eval/main.go` fall back to `DefaultCouncilChairmanModel`, making the documented behaviour unreachable. Same mismatch in `docs/strategies.md`.
  - PR #211: `RefineTopK` documented with a floor of 3 but code only defaults when `0`, accepts 1/2.
  - PR #215: anonymisation doc-comment stricter than what code can guarantee (proposer drafts can leak model names in their content).
  - This is a continuation of the W19 "planned-vs-current confusion" theme — same root cause (docs and code reviewed by different mental models) but a new variant (comments-vs-behaviour, not docs-vs-implementation).
  - Suggest: extend `docs-maintainer` to also lint Go doc-comments against the registration code (`cmd/server/main.go`), or add a `tech-lead` review pattern for it.
  - Confidence: medium.

- **Meta-theme: `/fix-review` skill is not being invoked.** No commit since at least 2026-04-17 with "fix-review", "address Copilot", or "address review" in the message; the only "apply ... findings" commit is #207 ("apply dreaming W19 findings"). 22 unaddressed Copilot comments across 5 PRs corroborate that the prescribed `Copilot → /fix-review → squash merge` loop isn't running.
  - Suggest: clarify whether the workflow has changed (Copilot is non-blocking by design?) or whether the skill needs a stronger trigger. The CLAUDE.md workflow line "Address comments → `/fix-review` → squash merge" is currently aspirational.
  - Confidence: high.

## 3. Stale plans

- ✅ Clean. `.claude/plans/` contains only `README.md`. Workflow "delete plan after issue creation" still being followed.

## 4. Agent-memory health

- **`tech-lead/project_architecture.md`** (mtime 2026-05-10, 7 days old): ✅ Refreshed during W19 apply (#207). Now accurately reflects 7-strategy enum, slog adoption, graceful shutdown, test inventory, regex-validated UUIDs. One residual: the file itself flags that the previous "CORS is configurable" claim was wrong — current state has CORS *hardcoded* in `internal/api/handler.go:24`. This contradicts `known_issues.md` line 43 which marks CORS as ✅ resolved. **Cross-memory contradiction.**

- **`tech-lead/known_issues.md`** (mtime 2026-03-22, **56 days old**): W19 flagged this; the apply pass did not touch it. Same three "Critical" items still un-annotated:
  - "No API key validation at startup"
  - "Stage3SynthesizeFinal never returns an error"
  - "No graceful shutdown" — known to be resolved per `project_architecture.md:79-80` (`srv.Shutdown` with signal trap)
  - Plus the CORS contradiction noted above.
  - Suggest: a 20-minute audit pass marking each Critical/Medium item with `✅ PR #N` or removing it. The known_issues file is materially misleading and `tech-lead` will reason from it.
  - Severity: **medium**.

- **`go-security-reviewer/project_frontend_security_posture.md`** (mtime 2026-03-31, 47 days old): ❌ References two GitHub issues (#53, #54) as "Known open issues." Both are **CLOSED** (verified via `gh issue view`). Memory will direct the agent to flag already-fixed problems.
  - Suggest: replace the "Known open issues" block with a "Resolved" line, or rewrite from current state. The "Positive controls" and "Fixed in PR #52" sections are still accurate.
  - Severity: low.

- **`agent-memory/MEMORY.md` (top-level)**: still empty placeholder. Code-generator and code-simplifier subdirs exist but contain no files. Same observation as W19 — no project-wide shared memory has accreted.

- **No duplicates detected** across agents.

## 5. Skill / agent inventory

- **Agents (11):** unchanged from W19. W19's "overlap suspect" between `security-reviewer.md` and `go-security-reviewer.md` is now **resolved** — both agents now have explicit "Frontend security audit only." / "Backend (Go) security audit only." scope statements at the top of their `description:` frontmatter. ✅

- **Skills (11):** `apply-dreaming`, `backlog`, `find-bugs`, `fix-review`, `housekeeping`, `improve`, `lib`, `review-deps`, `revival`, `self-learn`, `ship`. `lib/` is helper scripts (`env.sh`, `rest.sh`), not a skill — keep or fold elsewhere.
  - `fix-review` appears unused (see Theme 2 meta-theme). Verify whether this is intentional.
  - No new skill obsolescence since W19.

- **Dreaming reports**: `.claude/dreaming/reports/2026-W20.md` exists as a **0-byte file** (mtime 2026-05-17 08:53). This dreaming pass is presumably meant to write to it; the cron created the placeholder but never executed the writer until now. Not an issue, just an artifact of how the cron is wired.

## 6. Patterns I noticed

- **Strategy fan-out outran cross-cutting hygiene.** Six new strategies (Majority, RankRefine, Debate, MoA, Delphi, plus the Stage 2 polymorphism refactor) shipped in 8 calendar days (2026-05-09 → 2026-05-10). Cross-cutting concerns that should have been touched once per strategy — replay derivation, `ConsensusW` semantics, frontend Markdown rendering, failure-mode emission — were touched zero times for the late arrivals. Each new strategy correctly added its own happy-path code; the orthogonal contracts drifted. Worth a tech-lead memory: *"A new strategy must update (a) `deriveStage2Kind` mapping, (b) `ConsensusW` semantics or doc, (c) Markdown rendering for any new content type, (d) failure surfacing for any new LLM call."*

- **W19 apply was partial.** Of W19's four "agent-memory health" findings, the apply touched only `project_architecture.md`. The other three (`known_issues.md` audit, `project_frontend_security_posture.md` open-issue re-verification, the unused agent-memory subdir collapse decision) were not actioned. This suggests `/apply-dreaming` is implemented as "apply the most actionable item per section" rather than "all findings" — or the loop simply stopped early. Worth checking the skill's implementation.

- **Copilot is the de-facto QA layer, not a fix-review trigger.** Every recent PR ships with 2–6 Copilot inline comments; none are addressed before merge. This is fine *if* it's a deliberate "Copilot is advisory" policy, but CLAUDE.md still reads as if `/fix-review` is mandatory. The doc and the practice should reconcile.

- **The `archive/v1` reference in CLAUDE.md remains accurate** — `git log` confirms the v1 codebase was not touched in 90 days, v2 is the only active line. No drift.

## 7. What I couldn't tell (need manual review)

- Whether the 22 unaddressed Copilot comments are deliberately deferred (Val triages and dismisses without commenting), or whether `/fix-review` is supposed to run but is being skipped. If the former, CLAUDE.md should drop the prescription.
- Whether the **conversation replay defect** (`deriveStage2Kind` always returning `peer_ranking`) is actually user-visible — that depends on whether `metadata.council_type` is persisted in `Conversation.JSON` for the new strategies and how `loadConversation` populates it. Quick smoke test: run Delphi, reload, check whether Stage 2 still renders as a Delphi timeline.
- Whether `ConsensusW=0.0` in Delphi/MoA/Debate is genuinely wrong or intentional ("not applicable for this strategy"). If intentional, the doc-comment on `Metadata.ConsensusW` should distinguish "zero = not applicable" from "zero = no agreement."
- Whether the empty `agent-memory/code-generator/` and `agent-memory/code-simplifier/` directories indicate a future intent (these agents don't yet write memories) or a setup artifact that should be removed.
- Whether `lib/` belongs in `.claude/skills/` at all.
